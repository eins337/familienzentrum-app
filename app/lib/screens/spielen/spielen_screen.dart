import 'package:flutter/material.dart';

class SpielenScreen extends StatelessWidget {
  const SpielenScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Spielen')));
}
