import 'package:flutter/material.dart';

class GruppenScreen extends StatelessWidget {
  const GruppenScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Gruppen')));
}
