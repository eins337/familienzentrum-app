import 'package:flutter/material.dart';

class AdminSickReportsScreen extends StatelessWidget {
  const AdminSickReportsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Krankmeldungen')));
}
