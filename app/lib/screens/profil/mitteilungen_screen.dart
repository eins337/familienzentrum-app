import 'package:flutter/material.dart';

class MitteilungenScreen extends StatelessWidget {
  const MitteilungenScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Mitteilungen')));
}
